#include <stdio.h>
#include "aluno.h"

int main (void)
{
	Aluno *a = NULL;
	int mat;
	CriaAlunoVazio(a);
	InsereDadosAluno(a);
	GetMat(a, &mat);
	printf("Matrícula adicionada: %d", mat);
	printf("Compilado!\n");
	return 0;
}
